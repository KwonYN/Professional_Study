// BOJ_6416_트리인가
#if 0
#pragma warning(disable: 4996)
#include <cstdio>
#include <algorithm>
using namespace std;

void operation() {
	int u, v;
	while (true) {
		while (true) {
			scanf("%d %d", &u, &v);
			if (u == 0 && v == 0) break;	// 테케 끝
			else if (u == -1 && v == -1) return; // 완전 끝
			else {
				;
			}
		}
	}
}

int main() {
	freopen("in.txt", "r", stdin);
	operation();
	return 0;
}
#endif